analysis muon_demo year Run2018;

object good_muon : Muon {
  pt > 30 GeV;
  abs(eta) < 2.4;
}

region signal {
  count(good_muon) >= 1;
}

define n_good_muon = count(good_muon);
alias lead_muon_pt = leading(good_muon).pt;

output n_good_muon;
output lead_muon_pt;
